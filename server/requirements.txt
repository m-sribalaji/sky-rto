fastapi==0.136.1
uvicorn[standard]==0.47.0
sqlalchemy==2.0.41
aiosqlite==0.21.0
pydantic==2.13.4
python-multipart==0.0.29
jinja2>=3.1.0
slowapi==0.1.9
# Only needed once the first device enrolls under the native_signer
# public-key scheme (deps.py _verify_ecdsa_signature) — imported lazily,
# not on every request, so it's safe to add now ahead of that rollout.
cryptography>=42.0.0